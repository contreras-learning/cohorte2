const PostgresqlModel = function(cliente){

    this.getAll = function (table) {
        return new Promise((resolve, reject) => {
            
        });
    }

    return this;
}

module.exports = PostgresqlModel;